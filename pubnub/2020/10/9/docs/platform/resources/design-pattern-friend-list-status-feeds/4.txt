Date date = new Date();JsonObject data = new JsonObject();data.put("author", "user-a");data.put("status", "I am reading about Advanced Channel Groups!");data.put("timestamp", date.getTime() / 1000);pubnub.publish()  .channel("ch-user-a-status")  .message(data)  .async(new PNCallback<PNPublishResult>() {    @Override    public void onResponse(PNPublishResult result, PNStatus status) {      if (status.isError()) {        System.out.println("error happened while publishing: "          + status.toString());      }      else {        System.out.println("message Published w/ timetoken "          + result.getTimetoken());      }    }  });show all 22 lineshide 7 linesCopy