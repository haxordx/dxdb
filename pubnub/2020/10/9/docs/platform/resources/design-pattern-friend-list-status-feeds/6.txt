# Publish a status messageimport timedata = {  'author': 'user-a',  'status': 'I am reading about Advanced Channel Groups!',  'timestamp': time.time()}try:  envelope = pubnub.publish()\    .channel("ch-user-b-present")\    .message(data)\    .sync()  print("Message published with timetoken %s" % envelope.result.timetoken)except PubNubException as e:  print("Operation failed w/ status: %s" % e)show all 18 lineshide 3 linesCopy