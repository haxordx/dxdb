// ************************************// * User A and User B become friends// ************************************// Add User B to User A's groups: Add ch-user-b-present to cg-user-a-friendspubnub.addChannelsToChannelGroup()  .channelGroup("cg-user-a-friends")  .channels(Arrays.asList("ch-user-b-present"))  .async(new PNCallback<PNChannelGroupsAddChannelResult>() {    @Override    public void onResponse(PNChannelGroupsAddChannelResult result, PNStatus status) {      if (status.isError()) {        System.out.println("Operation failed w/ status:" + status.getStatusCode());      }      else {        System.out.println("Channel added to channel group");      }    }  });// Add User B to User A's groups: ch-user-b-status to cg-user-a-status-feedpubnub.addChannelsToChannelGroup()  .channelGroup("cg-user-a-status-feed")  .channels(Arrays.asList("ch-user-b-status"))  .async(new PNCallback<PNChannelGroupsAddChannelResult>() {    @Override    public void onResponse(PNChannelGroupsAddChannelResult result, PNStatus status) {      if (status.isError()) {        System.out.println("Operation failed w/ status:" + status.getStatusCode());      }      else {        System.out.println("Channel added to channel group");      }    }  });// Add User A to User B's groups: Add ch-user-a-present to cg-user-b-friendspubnub.addChannelsToChannelGroup()  .channelGroup("cg-user-b-friends")  .channels(Arrays.asList("ch-user-a-present"))  .async(new PNCallback<PNChannelGroupsAddChannelResult>() {    @Override    public void onResponse(PNChannelGroupsAddChannelResult result, PNStatus status) {      if (status.isError()) {        System.out.println("Operation failed w/ status:" + status.getStatusCode());      }else {        System.out.println("Channel added to channel group");      }    }  });// Add User B to User A's groups: ch-user-a-status to cg-user-b-status-feedpubnub.addChannelsToChannelGroup()  .channelGroup("cg-user-b-status-feed")  .channels(Arrays.asList("ch-user-a-status"))  .async(new PNCallback<PNChannelGroupsAddChannelResult>() {    @Override    public void onResponse(PNChannelGroupsAddChannelResult result, PNStatus status) {      if (status.isError()) {        System.out.println("Operation failed w/ status:" + status.getStatusCode());      }      else {        System.out.println("Channel added to channel group");      }    }  });show all 67 lineshide 52 linesCopy