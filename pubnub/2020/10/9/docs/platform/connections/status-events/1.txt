pubnub.addListener({    status: function(event) {        var affectedChannelGroups = event.affectedChannelGroups;        var affectedChannels = event.affectedChannels;        var category = event.category;        var operation = event.operation;        var lastTimetoken = event.lastTimetoken;        var currentTimetoken = event.currentTimetoken;        var subscribedChannels = event.subscribedChannels;    }});Copy