pubnub.messageCounts()  .channels(Arrays.asList("chats.room1", "chats.room2"))  .channelsTimetoken(Arrays.asList(15495750401727535L))  .async(new PNCallback<PNMessageCountResult>() {    @Override    public void onResponse(PNMessageCountResult result, PNStatus status) {      if (!status.isError()) {        for (Map.Entry<String, Long> entry : result.getChannels().entrySet()) {          entry.getKey(); // the channel name          entry.getValue(); // number of messages for that channel        }      }      else {        status.getErrorData().getThrowable().printStackTrace();      }    }  });show all 17 lineshide 2 linesCopy