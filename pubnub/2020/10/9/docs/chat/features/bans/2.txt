pubnub.grant()    .channels(Arrays.asList("ch-1")) //channels to allow grant on    .authKeys(Arrays.asList("key1")) // the keys we are provisioning    .write(false) // allow those keys to write (false by default)    .read(false) // allow keys to read the subscribe feed (false by default)    .async(new PNCallback<PNAccessManagerGrantResult>() {        @Override        public void onResponse(PNAccessManagerGrantResult result, PNStatus status) {            // PNAccessManagerGrantResult is a parsed and abstracted response from server        }    });Copy