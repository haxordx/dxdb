JsonObject state = new JsonObject();state.addProperty("mood", "grumpy");pubNub.setPresenceState()        .state(state)        .channels(Arrays.asList("room-1"))        .async(new PNCallback<PNSetStateResult>() {            @Override            public void onResponse(PNSetStateResult result, PNStatus status) {                if (!status.isError()) {                    // handle state setting response                }            }        });Copy